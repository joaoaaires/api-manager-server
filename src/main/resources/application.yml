spring:
  datasource:
    url: jdbc:h2:file:./data
    username: sa
    password: 123321
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
      format-sql: true
    show-sql: true

server:
  port: ${PORT:8080}
